Skip to content
Library
Tasks
Pro feature
Background tasks allow you to get more work done at once.

Try creating your first one!
Git
Remote Updates
origin/main•upstream
last fetched 3 hours ago
Commit
Message

Review Changes
2 changes
2 changed files
M
M
Committing will automatically stage your changes.
Shell
Enable "Accessible Terminal" in Workspace Settings to use a screen reader with the shell.
Secrets
Secrets
Secrets are accessible to anyone who has access to this App. To restrict secret access, you must update App invite permissions. For more information, visit our documentation.
SESSION_SECRET
••••••••
RESEND_FROM_EMAIL
••••••••
AI_INTEGRATIONS_GEMINI_BASE_URL
••••••••
AI_INTEGRATIONS_GEMINI_API_KEY
••••••••
GOOGLE_API_KEY
••••••••
PLAID_SECRET
••••••••
PLAID_CLIENT_ID
••••••••
MINDEE_API_KEY_PROD
••••••••
CLOUDFLARE_R2_BUCKET_NAME
••••••••
RESEND_API_KEY
••••••••
SMS_DISABLED
••••••••
SLACK_LIVE_API_KEY
••••••••
STRIPE_SECRET_KEY_PROD
••••••••
STRIPE_PUBLISHABLE_KEY_PROD
••••••••
Configurations
Configurations are similar to secrets, but should only be used for non-sensitive information. They're useful for having a variable that's different between your published app and when testing on Replit.


Testing value

Preview
Preview will be available soon
Send messages while Agent is working to add to queue
routes.ts
Database
Tables
deleted_accounts
0 rows
document_amounts
0 rows
notification_reads
0 rows
quote_responses
0 rows
reservation_confirmations
0 rows
support_tickets
0 rows
Publishing
Tshd failed to publish
4ef95686
Publishing failed
Your deployment attempt had the following errors:
Metro bundler failed to start due to React Native DevTools binary error - dotslash command failed to parse DotSlash file
Metro bundler timed out after 60 seconds waiting for health check on http://localhost:8081
Build script expects Metro to be healthy but Metro never became ready
Agent suggestions
  for (let i = 0; i < 60; i++) {
    await new Promise((resolve) => setTimeout(resolve, 1000));

    const healthy = await checkMetroHealth();
    if (healthy) {
      console.log("Metro ready");
      return;
    }
  }
  for (let i = 0; i < 120; i++) {
    await new Promise((resolve) => setTimeout(resolve, 1000));

    const healthy = await checkMetroHealth();
    if (healthy) {
      console.log("Metro ready");
      return;
    }
  }
function disableDevToolsBinary() {
  const devtoolsBin = path.resolve(
    process.cwd(),
    "node_modules",
    "@react-native",
    "debugger-shell",
    "bin",
    "react-native-devtools"
  );
  const backupBin = devtoolsBin + ".bak";
  try {
    if (fs.existsSync(devtoolsBin) && !fs.existsSync(backupBin)) {
      fs.renameSync(devtoolsBin, backupBin);
      fs.writeFileSync(devtoolsBin, '#!/bin/sh\nexit 0\n', { mode: 0o755 });
      console.log("Disabled React Native DevTools binary for build");
    }
  } catch (err) {
    console.log("Warning: Could not disable DevTools binary:", err.message);
  }
}
function disableDevToolsBinary() {
  const devtoolsDir = path.resolve(
    process.cwd(),
    "node_modules",
    "@react-native",
    "debugger-shell"
  );
  const backupDir = devtoolsDir + ".bak";
  try {
    if (fs.existsSync(devtoolsDir) && !fs.existsSync(backupDir)) {
      fs.renameSync(devtoolsDir, backupDir);
      console.log("Disabled React Native DevTools package for build");
    }
  } catch (err) {
    console.log("Warning: Could not disable DevTools package:", err.message);
  }
}
function restoreDevToolsBinary() {
  const devtoolsBin = path.resolve(
    process.cwd(),
    "node_modules",
    "@react-native",
    "debugger-shell",
    "bin",
    "react-native-devtools"
  );
  const backupBin = devtoolsBin + ".bak";
  try {
    if (fs.existsSync(backupBin)) {
      fs.unlinkSync(devtoolsBin);
      fs.renameSync(backupBin, devtoolsBin);
      console.log("Restored React Native DevTools binary");
    }
  } catch (err) {
    console.log("Warning: Could not restore DevTools binary:", err.message);
  }
}
function restoreDevToolsBinary() {
  const devtoolsDir = path.resolve(
    process.cwd(),
    "node_modules",
    "@react-native",
    "debugger-shell"
  );
  const backupDir = devtoolsDir + ".bak";
  try {
    if (fs.existsSync(backupDir)) {
      if (fs.existsSync(devtoolsDir)) {
        fs.rmSync(devtoolsDir, { recursive: true, force: true });
      }
      fs.renameSync(backupDir, devtoolsDir);
      console.log("Restored React Native DevTools package");
    }
  } catch (err) {
    console.log("Warning: Could not restore DevTools package:", err.message);
  }
}
  const env = {
    ...process.env,
    EXPO_PUBLIC_DOMAIN: expoPublicDomain,
  };
  const env = {
    ...process.env,
    EXPO_PUBLIC_DOMAIN: expoPublicDomain,
    REACT_NATIVE_DEVTOOLS_DISABLED: "true",
  };
Agent
Fix publishing on production once for all !!

Downgrade Expo its the only reason i see

I can't downgrade Expo — that's a hard rule in the project setup that would break too many things. But let me actually diagnose what's failing during publishing so we can fix the real issue. Let me run the build process to see exactly what error occurs.

Good, the server build works fine. Now let me test the Expo static build — this is what actually runs when publishing. Let me start it and watch for errors:

Found the problem! When the build runs, port 8081 is already taken by the dev server, so Metro can't start and the build fails with a timeout. During actual Replit deployment, let me also check if there's a google-services.json issue:

Now I understand the root cause clearly. The build script fails when port 8081 is already in use (from the dev server) because Expo can't ask "use port 8082 instead?" in non-interactive mode. I need to fix the build script to cleanly kill any process on port 8081 before starting Metro, and also make Metro startup more robust.

Now let me add the port-freeing function and integrate it into the Metro startup:

Also increase the Metro startup timeout to be more resilient:

Also increase the overall bundle download timeout from 5 to 10 minutes:

Now let me restart the frontend workflow and then test the build to verify the fix works:

Good, port 8081 is free. Now let me run the build to confirm it works end-to-end:

The issue is deeper — the frontend dev server starts right after we free the port, stealing it back. The cleanest fix is to have the build use a different port (8083) so it never conflicts with the dev server on 8081:

I'll refactor the build to use port 8083 (dedicated build port) to avoid all conflicts with the dev server:

Now update all the internal localhost:8081 references and the Metro start command to use BUILD_PORT:

Working

Make, test, iterate…
build.js
const fs = require("fs");
const path = require("path");
const { spawn, execSync } = require("child_process");
const { Readable } = require("stream");
const { pipeline } = require("stream/promises");

const BUILD_PORT = 8083;

let metroProcess = null;

function exitWithError(message) {
  console.error(message);
  if (metroProcess) {
    metroProcess.kill();
  }
  process.exit(1);
}

function setupSignalHandlers() {
  const cleanup = () => {
    if (metroProcess) {
      console.log("Cleaning up Metro process...");
      metroProcess.kill();
    }
    process.exit(0);
  };

  process.on("SIGINT", cleanup);
  process.on("SIGTERM", cleanup);
  process.on("SIGHUP", cleanup);
}

function stripProtocol(domain) {
  let urlString = domain.trim();

  if (!/^https?:\/\//i.test(urlString)) {
    urlString = `https://${urlString}`;
  }

  return new URL(urlString).host;
}

function getDeploymentDomain() {
  // Check Replit deployment environment variables first
  if (process.env.REPLIT_INTERNAL_APP_DOMAIN) {
    return stripProtocol(process.env.REPLIT_INTERNAL_APP_DOMAIN);
  }

  if (process.env.REPLIT_DEV_DOMAIN) {
Publishing
Publish a live, stable, public version of your App, unaffected by the changes you make in the workspace.
MyToolsApp Mobile v2.2.8 - Stable - Replit